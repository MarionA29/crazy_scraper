require_relative "../lib/townhall_spec.rb"

describe "the perform method" do
  it "should return an array with the townhalls' names and emails" do
    expect(Hash).not_to be_nil
  end
end


describe "the perform method" do
  it "should return an array with the townhalls' names and emails" do
    expect(Hash[0]).not_to be_nil
  end
end

describe "the perform method" do
  it "should return an array with the townhalls' names and emails" do
    expect(["villiers-le-bel"]).not_to be_nil
  end
end
